ungrib:
  execution:
    executable: ungrib
  gribfiles:
    - "{{ runroot }}/01/anl.grib2"
  rundir: '{{ runroot }}/02'
  start: "{{ cycle }}"
  step: 1
  stop: "{{ cycle }}"
  vtable: '{{ "WPSFILES" | env }}/Vtable.GFS'
